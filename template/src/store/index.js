import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const defaultState = {
}

const mutations = {
}

const actions = {
}

const getters = {
}

const store = new Vuex.Store({
    state: defaultState,
    getters,
    actions,
    mutations,
})

export default store
